# Build context exclusions — production image never ships secrets or build junk.
/target/
*.log
*.toml.orig
*.bak
*.swp
.DS_Store

# Never bake prod credentials into the image
config.prod.toml
config.debug.toml

# Keep docs out of the slim runtime layer
LICENSE
README.md
docs/
