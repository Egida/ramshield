# Daily gauntlet 2026-09-24

Prod: pid 3677514, XDP active (caps eip, snapshot xdp_active True), errs=0, RSS 72 MB (was 38 MB pre-round; leftover state + this round). No relaunch. Sync: already-current (`origin/master` ancestor of `daily-gauntlet`).

Harness: driver/sampler already running; driver also consumed leftover 121–130 (old C/D shapes) before timeout on 131. Today's ten shapes run as 142–151, sequential, RFC5737/3849 only.

| # | shape | load | key numbers | verdict |
|---|-------|------|-------------|---------|
| E1 | 32w block+unblock churn | 138958 ops / 50s | 2724.5 ops/s, fail=0, apply_fail=0, rss+584kB | HOLD (A4 ~1748 ops/s uncontended; this faster) |
| E2 | CIDR nest 203.0.113 /24–/28 | 40 cycles, 5 probes | blocked=5/5, apply_fail=0, gaps=0 | HOLD |
| E3 | pulse 2s-on/2s-off @~1.5k eps, 40 IPs 192.0.2 | 75520 ev | shed=0, ingest 6.3M cum; blocks_delta metric missing (`ramshield_blocks_applied_total` absent) | HOLD ingest; metric name drift on that series |
| E4 | 400 blocks ttl=8s, check after 12s | 400 write 0.18s | still_blocked=0/50 samples | HOLD (expiry) |
| E5 | HMAC replay identical frame | 2 sends | first_ok=1, replay_hit=1 | HOLD |
| E6 | 8-conn pipelined check_ip | 52577 ops / 45s | 1155.5/s, fail=8 | HOLD (fails=reconnects; A2 16conn was 18k/s clean) |
| E7 | 2k IPv6 2001:db8:1:x::1 blocks | 2000 | 1796.8/s, err=0, probe_blocked=1 | HOLD |
| E8 | /metrics 1s cache | 2×fast + block + 1.3s | same_fast=1; `ramshield_blocks_applied_total` **None** (series not exported under that name) | FLAG: metric key missing; cache unproven on that series |
| E9 | proto_fp missing vs 0 | 2 frames | missing → type=error code=1, rej+1; proto_fp=0 → batch_ok | HOLD |
| E10 | block_cidr /24 then check member | 1 /24 | member blocked reason=`cidr_block(198.51.170.0/24)`, outside clean | HOLD |

Anomalies: E3/E8 — Prometheus lacks `ramshield_blocks_applied_total` (None). E6 8 reconnect fails (not zero). RSS +34 MB vs pre-flight (prior leftover queue 121–127 plus E1/E7).

Defects flagged (no fix this path): missing `ramshield_blocks_applied_total` in /metrics (use live ingest/check_ip). Driver `done` set empty on restart → re-ran historical 121–130 against prod (noise, not product).
